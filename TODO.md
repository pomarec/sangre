 - Accept sync or async methods in NodeOperators and such
 - Fix types system (no longer use "as" keyword)
 - Example with typed model data
 - Reuse existing nodes
    - Needs to "static"ify nodeId computation